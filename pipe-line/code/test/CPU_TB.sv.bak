module CPU_TB();
	integer EndCnt = 0;

	reg Clk;

	initial Clk = 0;
	always #200 Clk = ~Clk;
	CPU OUR_CPU(Clk);
	always begin
		#207;
		if(OUR_CPU.DP.Wires[1] == 32'd64) begin
			EndCnt = EndCnt + 1;
		end else begin
			EndCnt = 0;
		end

		if(EndCnt == 2) begin
			$stop;
		end
	end
endmodule
